# تحقق إصلاح التمرير

أزيلت خاصية `autoPlay` من عنصر الفيديو وأزيل تشغيل `video.play()` من مسار التمرير. أصبح الفيديو متوقفًا افتراضيًا، ولا يُحدّث `currentTime` إلا بعد تغير target progress الناتج عن scroll أو resize.

تم فصل `targetProgress` عن `smoothedProgress`، واستُخدمت حلقة requestAnimationFrame مؤقتة تعمل فقط أثناء وجود فرق بين الهدف والعرض. تستخدم الحلقة تنعيمًا زمنيًا `1 - Math.exp(-dt * 11)`، وتوقف نفسها عند الوصول للهدف. يتم تحديث الفيديو عند فرق زمني صغير فقط، مع إبقاء العنصر paused طوال scrubbing.

نجح `pnpm check` و`pnpm build`. لقطات سطح المكتب والهاتف ظهرت دون أخطاء ظاهرة، مع بقاء تجربة المشهد والبطاقات مقروءة.
