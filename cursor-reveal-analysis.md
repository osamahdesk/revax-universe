# تحليل تسجيل الشاشة — الفيديو وكشف الصور

## الملاحظات المرصودة

يظهر التعليق عند الانتقال من الـ Hero إلى قسم Atmospheric Study. التوقيت يتزامن مع تحميل/عرض أصول مرئية جديدة ومعالجة blur وopacity وtransform المرتبطة بالتمرير. تتأخر البطاقات قليلًا عند أول تفاعل، بينما التبديل الحالي لا يزال انتقالًا كاملًا بدل كشف محلي.

## قرار الإصلاح

سيتم تثبيت طبقة النص والبطاقة، ووضع صورتين لكل بطاقة في طبقتين مطلقتين. الطبقة الثانية ستُكشف فقط داخل دائرة تتبع المؤشر باستخدام `clip-path: circle(...)` أو `mask`, مع تحديث الإحداثيات عبر requestAnimationFrame واحد. خارج الدائرة تبقى الصورة الأساسية، وداخل الدائرة تظهر الصورة البديلة. على اللمس سيظهر التكوين الأساسي دون مؤشر مخصص أو كشف دائري.

## تقليل التقطيع

سيُمنع تحديثات state في كل mousemove مباشرة؛ سيُحفظ آخر موضع للمؤشر في ref، وتُطبّق قيم CSS مرة واحدة لكل frame. كما ستُستخدم `will-change: clip-path`، وتُخفف عمليات blur الثقيلة أثناء التفاعل. يظل فيديو الخلفية paused ويُحدّث فقط من حلقة scroll الحالية.
