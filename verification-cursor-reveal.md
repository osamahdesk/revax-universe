# تحقق كشف الصور داخل المؤشر

تم تحويل كل بطاقة إلى طبقتين: صورة أساسية وصورة بديلة. الصورة البديلة تُكشف فقط داخل دائرة قطرها 108px حول موضع المؤشر داخل البطاقة، باستخدام `clip-path` ومتغيري CSS `--cursor-x` و`--cursor-y`. يتم تحديث الإحداثيات عبر requestAnimationFrame واحد لكل بطاقة بدل setState مع كل mousemove، مما يقلل إعادة الرسم.

تظل النصوص والأرقام والطبقة المتدرجة ثابتة فوق الصور. على الأجهزة اللمسية يُخفى الكشف الموضعي وتبقى الصورة الأساسية؛ لا يوجد مؤشر مخصص أو hover يعتمد عليه. نجح `pnpm check` و`pnpm build`، وظهرت الصفحة بصورة مستقرة في لقطتي سطح المكتب والهاتف.
