# تحليل تسجيل الشاشة — إصلاح بداية الموشن

## الملاحظات المباشرة

يظهر الـ Hero أولًا مع خلفية كونية وفقاعة Scroll ثابتة. عند أول تمرير تبدأ البطاقات والاستجابة الخلفية، لكن توجد قفزة بسيطة عند الانتقال من السكون إلى التفاعل. عند دخول القسم sticky يظهر تجميد لحظي قرب انتقال المشهد الأول، بينما يستمر النص في الحركة. عند الانتقال للمشهد الثاني تصبح حركة الفيديو متقطعة، كأن currentTime يقفز بين إطارات. في النهاية يستقر المشهد قرب التذييل وتختفي الفقاعة أو تصبح غير فعالة.

## التشخيص

السبب الأرجح هو اختلاف الحالة الابتدائية للفيديو عن target progress عند بداية القسم sticky، مع مزامنة currentTime أثناء فترات لا يوجد فيها تمرير فعلي. كما أن حلقة التمرير السابقة كانت تبدأ بتحديث target عند mount، وتسمح بتغيرات متتالية في currentTime دون قفل واضح على أول إطار. هذا يخلق jump في البداية وsteppy playback أثناء التمرير السريع.

## قرار الإصلاح

يجب أن يبدأ الفيديو عند `currentTime = 0` ويبقى paused قبل أول scroll فعلي. عند اكتشاف أول scroll يبدأ target progress من الصفر ويُفعّل scrub ناعم فقط أثناء انتقال display إلى target. لا يستخدم المسار `video.play()` مطلقًا. يجب تثبيت فقاعة Scroll في الـ Hero وإخفاؤها تدريجيًا عند أول 100px من التمرير، مع تحويلها إلى liquid neon glass كوني بألوان `#a8e8ff` ودرجات navy.

## تنفيذ متوقع

استخدام lerp زمني مستقل عن سرعة أحداث scroll، مع requestAnimationFrame يعمل أثناء وجود فرق فقط، وتحديث currentTime بعتبة صغيرة. يتوقف الفيديو عندما يصل display إلى target، ويُعاد ضبطه إلى الإطار صفر فقط عند العودة إلى أعلى الصفحة إذا لم يبدأ المستخدم المشهد بعد.
